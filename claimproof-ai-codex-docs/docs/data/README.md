# Mock Data

This directory is the canonical mock data source for the ClaimProof AI MVP.

Implementation may copy these files to `src/data`, but should not change values unless the design spec is explicitly updated.
